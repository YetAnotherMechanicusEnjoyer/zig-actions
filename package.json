{
  "name": "zig-actions",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:ci": "ncc build src/ci/main.ts -o ci/dist --source-map --license licenses.txt",
    "build": "npm run build:ci"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/exec": "^1.1.1",
    "@actions/tool-cache": "^2.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.0.0"
  }
}
